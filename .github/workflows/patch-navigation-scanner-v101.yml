name: Patch navigation scanner v1.0.1

on:
  push:
    branches:
      - dev
    paths:
      - '.github/workflows/patch-navigation-scanner-v101.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - name: Apply verified scanner patch
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object Jellyfin.Plugin.QoL/Web/navigationScanner.js)" = "f9f15e277c3e8bc0ec82f32a31915759dffeda50"
          git apply --check .github/patches/navigationScanner-v101.patch
          git apply .github/patches/navigationScanner-v101.patch
          test "$(git hash-object Jellyfin.Plugin.QoL/Web/navigationScanner.js)" = "3ccef135b0862248d606b8be7acc84697910f9f5"

      - name: Commit source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Jellyfin.Plugin.QoL/Web/navigationScanner.js
          git commit -m "fix: refine scanner audit and active-page discovery"
          git push origin HEAD:dev
